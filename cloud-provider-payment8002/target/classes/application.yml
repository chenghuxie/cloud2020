server:
  port: 8002


spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://rm-uf65ewl375287v916.mysql.rds.aliyuncs.com:3306/demo?useUnicode=true&amp;characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull
    username: jdwhy
    password: Prejd_2017
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.xch.com.xch.springcloud.entities

eureka:
   client:
   #表示是否将自己注册进EurekaServer  默认为true
     register-with-eureka: true
     #是否从EurekaServer抓取已有的注册信息，默认为true，单节点无所谓，集群必须设置为true才能配合ribbon使用负载均衡
     fetch-registry: true
     service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
   instance:
     instance-id: payment8002
     prefer-ip-address: true


